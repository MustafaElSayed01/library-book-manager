package com.library.model;

/**
 * Enumerates the availability states a book can have in the catalog.
 *
 * <p>The values in this enum should support the borrow and return workflows exposed
 * by the console application.</p>
 */
public enum BookStatus {
    // TODO: Define availability statuses (AVAILABLE, BORROWED)
}
